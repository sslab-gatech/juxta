INFO
====
- This directory contains the unit tests. All the tests are in a
  separate directory with a simple name test.c and Makefile
  symbolic link.
- The patches include the commit ids of the patches from which
  they are inspired.
- The section inside __PATCH__ macro is the actual patch
  that has been committed.


PATCHES TYPE
============

Following patches are/will be present in this directory as numbered:

1) Missing one unique return value.
2) Missing a non-unique return value.
3) Variable update.
4) Missing break statement.
5) Incorrect placement of variable update.
6) Wrong variable type
7) goto condition
8) redundant repetition of funciton/MACRO.
9) Missing Initialization (like variable update)
10)Wrong path condition
